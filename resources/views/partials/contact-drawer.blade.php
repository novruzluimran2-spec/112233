<div class="muted small">Тел: <a href="tel:{{ config('restaurant.phone') }}">{{ config('restaurant.phone_display') }}</a></div>
<div class="muted small">Email: <a href="mailto:{{ config('restaurant.email') }}">{{ config('restaurant.email') }}</a></div>
